import React from 'react';
import Game from 'features/game/Game';
import Layout from 'containers/Layout';

const Quiz = () => {
  return (
    <Layout>
      <Game />
    </Layout>
  );
};

export default Quiz;
